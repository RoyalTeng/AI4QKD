# AI4QKD 深度学习环境配置指南（D盘安装）

## 前置要求

### 1. 安装Python（推荐安装到D盘）

1. 访问 [Python官网](https://www.python.org/downloads/) 下载Python 3.9-3.11版本
2. 运行安装程序时：
   - ✅ 勾选 "Add Python to PATH"
   - ✅ 选择 "Customize installation"
   - ✅ 安装路径设置为：`D:\Python`
3. 验证安装：打开CMD运行 `python --version`

### 2. 安装NVIDIA驱动（如有GPU）

1. 访问 [NVIDIA官网](https://www.nvidia.com/drivers/) 下载最新驱动
2. 下载并安装 [CUDA Toolkit 12.1](https://developer.nvidia.com/cuda-downloads)
3. 验证安装：打开CMD运行 `nvidia-smi`

## 自动配置环境

### 方法1：使用批处理脚本（推荐）

```batch
# 在F:\AI4QKD目录下运行：
setup_environment.bat
```

### 方法2：手动配置

```batch
# 1. 创建虚拟环境
mkdir D:\AI4QKD_env
python -m venv D:\AI4QKD_env\venv

# 2. 激活虚拟环境
D:\AI4QKD_env\venv\Scripts\activate

# 3. 升级pip
python -m pip install --upgrade pip

# 4. 配置pip缓存到D盘
pip config set global.cache-dir D:\AI4QKD_env\pip_cache

# 5. 安装PyTorch (GPU版本)
pip install torch==2.5.1+cu121 torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

# 或安装CPU版本
pip install torch==2.5.1+cpu torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu

# 6. 安装项目依赖
pip install -r requirements.txt

# 7. 以开发模式安装
pip install -e .
```

## 验证安装

```python
# 运行Python测试
python -c "import torch; print(f'PyTorch版本: {torch.__version__}'); print(f'CUDA可用: {torch.cuda.is_available()}'); print(f'GPU数量: {torch.cuda.device_count()}')"

# 运行项目测试
pytest tests/ -v

# 运行示例
python examples/bb84_example.py
```

## 日常使用

### 激活环境

```batch
# 方法1：使用快捷脚本
activate_env.bat

# 方法2：手动激活
D:\AI4QKD_env\venv\Scripts\activate
cd F:\AI4QKD
```

### 运行训练

```python
# 主训练程序
python main.py

# 自定义运行ID
python main.py --run_id my_experiment

# 从检查点恢复
python main.py --resume_id previous_run_id
```

## 目录结构（D盘）

```
D:\AI4QKD_env\
├── venv\                    # 虚拟环境
├── pip_cache\               # pip缓存
└── models\                  # 模型缓存（可选）
```

## 常见问题

### 1. Python找不到
- 确保Python已添加到PATH环境变量
- 重启命令行窗口

### 2. CUDA版本不匹配
- 检查NVIDIA驱动版本：`nvidia-smi`
- 安装对应的PyTorch版本

### 3. 内存不足
- 使用pip缓存：`pip config set global.cache-dir D:\AI4QKD_env\pip_cache`
- 分批安装依赖包

### 4. 权限问题
- 以管理员身份运行命令行
- 检查D盘写入权限

## 性能优化建议

1. **GPU加速**：确保安装了CUDA版本的PyTorch
2. **内存管理**：设置环境变量 `export CUDA_VISIBLE_DEVICES=0`
3. **并行计算**：在config中调整worker数量
4. **缓存优化**：将pip缓存和模型缓存放在D盘