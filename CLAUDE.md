# CLAUDE.md

此文件为Claude Code (claude.ai/code) 在此代码库中工作时提供指导。

## 项目概述

AI4QKD是一个AI辅助量子密钥分发协议设计系统，该项目目前处于**完全重构阶段**。原有的复杂代码已在`clean-start-v3`分支中完全清空，只保留了核心参考文献和研究方案。

### 项目状态
- **当前分支**: `clean-start-v3` - 全新开始的重构分支
- **代码状态**: 已清空，只保留文档和参考文献
- **重构目标**: 从20,000+行复杂代码简化为简洁系统
- **重构策略**: 基于GitHub master分支参考，按重构目标重新实现

## 重构策略说明

### 基于参考的重构方法
- **参考代码**: GitHub master分支 (https://github.com/RoyalTeng/AI4QKD.git)
- **重构原则**: 参考原有代码思路和设计模式，按重构目标重新实现
- **保持连续性**: 维持设计思路的连续性，避免重复造轮子
- **优化实现**: 在原有基础上优化性能和可维护性

### 重构工作流要求

每个重构任务都必须严格遵循以下5步流程：

#### 1. 🧠 探索与规划 (Think)
- **分析GitHub代码**: 深入分析对应模块的代码思路和架构
- **识别设计模式**: 理解原有的设计模式和实现思路
- **确定重构目标**: 明确需要改进的具体方面
- **制定重构计划**: 基于参考制定详细的重构计划

#### 2. 🧪 测试先行 (Test First)
- **参考原有接口**: 基于原有接口设计编写测试
- **保持兼容性**: 确保新实现与原有接口兼容
- **定义新行为**: 为新的优化功能编写测试
- **建立基准**: 建立性能和质量基准

#### 3. 💻 编码实现 (Code)
- **参考实现思路**: 参考原有的实现思路和算法
- **重新实现**: 按照重构目标重新编写代码
- **保持一致性**: 保持与项目整体风格的一致性
- **优化性能**: 在保持思路的基础上优化性能

#### 4. 🔄 多轮迭代 (Refactor)
- **简化逻辑**: 简化复杂的实现逻辑
- **优化结构**: 优化代码结构和组织
- **提升质量**: 提升代码质量和可读性
- **确保兼容**: 确保与原有系统的兼容性

#### 5. ✅ 提交与反馈 (Commit)
- **对比分析**: 对比原有实现和新实现
- **记录改进**: 记录具体的改进点和收益
- **提供总结**: 提供详细的重构总结
- **准备交接**: 准备下一个模块的重构

## 代码注释与重构思路文档要求

### 代码注释规范

#### 1. 文件头部注释
每个Python文件必须在开头包含详细的模块说明：

```python
"""
AI4QKD - [模块名称]

重构思路：
- 参考GitHub master分支的[具体文件]实现思路
- 简化了[具体简化点]，提升了[具体改进]
- 保持了[具体保持的设计思路]

设计原则：
- [设计原则1]
- [设计原则2]
- [设计原则3]

主要改进：
- [改进点1]
- [改进点2]
- [改进点3]

作者: [您的姓名]
重构日期: [日期]
参考版本: GitHub master分支
"""

# 导入语句...
```

#### 2. 类级别注释
每个类必须有详细的设计思路说明：

```python
class ProtocolGraph:
    """
    QKD协议图表示类 - 重构版本
    
    重构思路：
    - 参考原有ProtocolGraph类的图结构设计
    - 简化了节点和边的管理逻辑
    - 优化了序列化和反序列化性能
    
    设计改进：
    - 使用更简洁的数据结构
    - 统一了节点类型系统
    - 改进了错误处理机制
    
    核心功能：
    - 协议图的创建和管理
    - 节点和边的操作
    - 协议图的序列化
    
    使用示例：
        protocol = ProtocolGraph("BB84")
        node_id = protocol.add_node(NodeType.QSP, "Alice")
        edge_id = protocol.add_edge(node1, node2, "quantum")
    """
    
    def __init__(self, name: str):
        """
        初始化协议图
        
        重构思路：
        - 简化了初始化参数
        - 使用更直观的数据结构
        - 改进了内存使用效率
        
        Args:
            name: 协议名称
        """
        pass
```

#### 3. 方法级别注释
每个重要方法都必须包含重构思路：

```python
def add_node(self, node_type: NodeType, party: str, params: dict = None) -> str:
    """
    添加节点到协议图
    
    重构思路：
    - 参考原有add_node方法的参数设计
    - 简化了节点创建逻辑
    - 改进了ID生成机制
    - 优化了参数验证
    
    设计改进：
    - 使用更清晰的参数结构
    - 简化了节点类型检查
    - 改进了错误处理
    
    Args:
        node_type: 节点类型 (QSP, QM, QC等)
        party: 参与方 (Alice, Bob等)
        params: 节点参数
        
    Returns:
        str: 节点ID
        
    Raises:
        ValueError: 参数无效时抛出
    """
    pass
```

#### 4. 复杂逻辑注释
对于复杂的算法或逻辑，必须有详细的实现思路：

```python
def _calculate_key_rate(self, protocol_data: dict) -> float:
    """
    计算安全密钥率
    
    重构思路：
    - 参考原有密钥率计算算法
    - 简化了计算流程
    - 优化了数值稳定性
    - 改进了性能
    
    算法说明：
    1. 提取协议参数 (QBER, Gain等)
    2. 计算信息论安全边界
    3. 应用有限密钥修正
    4. 返回最终密钥率
    
    性能优化：
    - 使用向量化计算
    - 缓存中间结果
    - 优化内存使用
    """
    pass
```

### 重构思路文档

#### 1. 模块级重构文档
每个模块目录下创建 `REFACTOR_NOTES.md`：

```markdown
# [模块名称] 重构文档

## 重构概述
- **重构日期**: [日期]
- **参考版本**: GitHub master分支
- **重构目标**: [具体目标]
- **主要改进**: [改进点列表]

## 设计思路对比

### 原有设计
```python
# 原有代码示例
class OriginalClass:
    def complex_method(self):
        # 复杂的实现
        pass
```

### 重构后设计
```python
# 重构后代码示例
class RefactoredClass:
    def simplified_method(self):
        # 简化的实现
        pass
```

## 重构决策记录

### 决策1: [具体决策]
- **问题**: [遇到的问题]
- **原有方案**: [原有解决方案]
- **重构方案**: [重构后的解决方案]
- **理由**: [重构的理由和好处]

### 决策2: [具体决策]
- **问题**: [遇到的问题]
- **原有方案**: [原有解决方案]
- **重构方案**: [重构后的解决方案]
- **理由**: [重构的理由和好处]

## 性能对比

| 指标 | 原有版本 | 重构版本 | 改进幅度 |
|------|----------|----------|----------|
| 执行时间 | X ms | Y ms | Z% |
| 内存使用 | X MB | Y MB | Z% |
| 代码行数 | X 行 | Y 行 | Z% |

## 测试覆盖

- [ ] 单元测试覆盖率 > 90%
- [ ] 集成测试通过
- [ ] 性能测试达标
- [ ] 向后兼容性验证

## 下一步计划

- [ ] [具体任务1]
- [ ] [具体任务2]
- [ ] [具体任务3]
```

#### 2. 函数级重构注释
对于重要的重构函数，使用特殊注释标记：

```python
# ============================================================================
# 重构说明: 此函数基于GitHub master分支的[原函数名]重构
# 重构日期: [日期]
# 重构原因: [具体原因]
# 主要改进: [改进点]
# 参考文件: [GitHub上的原文件路径]
# ============================================================================

def refactored_function(self, param1, param2):
    """
    重构后的函数实现
    
    重构思路：
    - 参考原有[原函数名]的实现逻辑
    - 简化了[具体简化点]
    - 优化了[具体优化点]
    - 改进了[具体改进点]
    
    与原有实现的差异：
    - [差异点1]
    - [差异点2]
    - [差异点3]
    """
    pass
```

### 代码可读性要求

#### 1. 变量命名
```python
# 好的命名 - 清晰表达意图
quantum_state_preparation_node = protocol.add_node(NodeType.QSP, "Alice")

# 避免的命名 - 过于简短或模糊
qsp = p.add_node(NT.QSP, "A")
```

#### 2. 函数设计
```python
# 好的设计 - 单一职责，清晰输入输出
def calculate_secure_key_rate(protocol_data: dict) -> float:
    """计算安全密钥率"""
    pass

# 避免的设计 - 功能混杂，参数过多
def process_protocol(protocol, calculate_key=True, validate=True, optimize=True, ...):
    """处理协议（功能混杂）"""
    pass
```

#### 3. 错误处理
```python
# 好的错误处理 - 清晰的错误信息
def add_node(self, node_type: NodeType, party: str) -> str:
    if not isinstance(node_type, NodeType):
        raise ValueError(f"Invalid node type: {node_type}. Expected NodeType enum.")
    
    if party not in ["Alice", "Bob", "Eve"]:
        raise ValueError(f"Invalid party: {party}. Must be Alice, Bob, or Eve.")
```

### 文档更新要求

#### 1. 代码变更时同步更新
- 每次重构代码时，必须同步更新相关注释
- 重构思路文档要及时更新
- API文档要与代码保持同步

#### 2. 版本控制注释
```python
# 版本: v3.0 (重构版本)
# 变更: 简化了ProtocolGraph类的实现
# 日期: 2025-08-16
# 作者: [您的姓名]
```

## 核心架构设计（目标）

### 计划的简化架构
```
AI4QKD/
├── core/
│   ├── protocol.py      # QKD协议表示
│   ├── simulator.py     # 量子通信仿真  
│   ├── ai_designer.py   # AI驱动协议优化
│   └── evaluator.py     # 安全和性能分析
├── examples/
│   ├── bb84_demo.py     # 经典BB84演示
│   └── ai_training.py   # AI训练示例
├── utils/
│   ├── visualizer.py    # 协议可视化工具
│   └── config.py        # 系统配置
├── main.py              # 主入口程序
├── requirements.txt     # 最小依赖
└── tests/               # 单元测试
```

## 重构模块优先级

### 第一阶段：基础模块重构 
1. **qcgf_dsl** - 协议表示层（核心基础）
2. **config** - 配置管理（简单模块）
3. **utils** - 工具函数（通用模块）

### 第二阶段：核心功能重构 
4. **simulator** - 量子仿真器（核心功能）
5. **security_evaluator** - 安全评估（重要模块）
6. **ai_agent** - AI智能体（复杂模块）

### 第三阶段：集成与优化 
7. **tests** - 测试体系
8. **examples** - 示例代码
9. **docs** - 文档更新

## 开发环境配置

### Conda虚拟环境
- **环境名称**: ai4qkd_env
- **环境路径**: D:\Users\Administrator\miniconda3\envs\ai4qkd_env
- **Python版本**: 3.8+
- **状态**: 已创建并安装依赖
- **使用方式**: 整个重构过程都使用此环境

### 环境激活命令
```bash
# 激活conda环境
conda activate ai4qkd_env

# 或者直接指定路径激活
conda activate D:\Users\Administrator\miniconda3\envs\ai4qkd_env

# 验证环境
python --version
pip list | grep -E "(numpy|scipy|matplotlib|qiskit|torch)"
```

### 环境要求
- 确保每次开发前都激活 `ai4qkd_env` 环境
- 环境路径：`D:\Users\Administrator\miniconda3\envs\ai4qkd_env`
- 所有依赖包都已预装，无需重新安装
- 使用此环境进行所有重构工作

### 环境验证
```bash
# 验证关键依赖
python -c "import numpy, scipy, matplotlib; print('科学计算库正常')"
python -c "import qiskit; print('量子计算库正常')"
python -c "import torch; print('深度学习库正常')"
```

## 开发命令（计划中）

### 环境设置
```bash
# 激活conda环境（每次开发前必须执行）
conda activate ai4qkd_env

# 验证环境状态
python --version
pip list | head -10

# 检查关键依赖
python -c "import numpy, scipy, matplotlib, qiskit, torch; print('所有依赖正常')"
```

### 测试命令
```bash
# 运行所有测试
python -m pytest tests/ -v

# 运行特定测试模块
python -m pytest tests/test_protocol.py -v
python -m pytest tests/test_simulator.py -v
python -m pytest tests/test_ai_designer.py -v

# 运行测试并显示覆盖率
python -m pytest tests/ --cov=core --cov-report=html
```

### 运行示例
```bash
# 基础演示
python main.py --demo

# BB84基准测试
python examples/bb84_demo.py

# AI训练演示
python examples/ai_training.py

# 性能基准测试
python benchmark.py --protocol BB84 --iterations 1000
```

### 代码质量
```bash
# 代码格式化
black .

# 代码检查
flake8 core/ examples/ tests/

# 类型检查
mypy core/
```

## 核心设计原则

### 1. 极简主义架构
- **单一职责**: 每个模块只做一件事
- **最小依赖**: 只使用必要的库 (numpy, matplotlib, 可选qiskit)
- **清晰接口**: 简单直观的API设计

### 2. 协议表示方法
- QKD协议表示为有向图：节点（量子操作）+ 边（通信链路）
- 使用简单的Python数据结构，避免复杂的图库依赖
- 支持序列化为JSON格式用于保存和加载

### 3. AI优化策略
- 使用遗传算法而非复杂的深度学习
- 专注于协议结构优化，不涉及量子电路优化
- 快速评估适应度，支持大规模进化搜索

### 4. 安全性评估
- 基于信息论安全的数学公式计算
- 有限密钥分析用于实际场景
- 实时安全验证确保生成协议的有效性

## 已验证的研究成果

### 性能基准
- **BB84基准**: 0.480900 bits/pulse
- **AI增强协议**: 0.505332 bits/pulse
- **性能提升**: 5.08%

### 关键创新
- 自适应态制备：动态量子态优化
- 智能基选择：机器学习指导的测量
- 协议图表示：使AI优化成为可能

## 理论基础

### 核心参考文献
1. **Gisin等 (2002)** - 量子密码学基础理论
2. **Dunjko & Briegel (2018)** - AI在量子领域的应用综述  
3. **现代物理评论 (2024)** - QKD协议优化的最新进展

### 研究方法
结合三个领域：量子密码学 + 机器学习 + 形式化验证

## 重构指导原则

### 删除的复杂功能
- ❌ 多层抽象框架 (universal_framework, ac_framework)
- ❌ 复杂的深度学习实现
- ❌ 重复的仿真器类
- ❌ 过度工程化的配置系统

### 保留的核心功能  
- ✅ QKD协议图表示
- ✅ 基本量子仿真
- ✅ AI协议优化
- ✅ 安全性评估
- ✅ 结果可视化

### 重构质量要求
- **代码行数减少**: 目标减少30-40%
- **复杂度降低**: 圈复杂度 < 10
- **测试覆盖率**: > 90%
- **文档完整性**: 100% API文档

### 性能目标
- **启动时间**: < 5秒
- **协议仿真**: 提升50%速度
- **内存使用**: 减少30%
- **训练效率**: 提升20%

## 开发工作流程

### 1. 核心模块开发
按以下顺序实现核心模块：
1. `protocol.py` - 协议表示和序列化
2. `simulator.py` - 快速性能仿真
3. `ai_designer.py` - 遗传算法优化
4. `evaluator.py` - 安全性评估

### 2. 测试驱动开发
- 每个模块先写测试后实现
- 使用pytest进行单元测试
- 集成测试验证端到端功能

### 3. 示例和文档
- 创建清晰的使用示例
- 保持文档与代码同步
- 提供可重现的实验结果

## 性能目标

### 代码量目标
- 总文件数: < 15个
- 总代码行数: 行数不做要求，只要代码思路尽可能简洁
- 主要模块: 4个核心 + 工具

### 功能目标
- 保持原系统90%的核心功能
- 新用户30分钟内理解项目
- 完整示例运行时间<1分钟

## 重构输出格式要求

### 探索与规划阶段输出：
```markdown
## 🧠 探索与规划

### GitHub代码分析
- [分析原有代码的架构和设计思路]
- [识别设计模式和实现优点]
- [理解核心算法和数据结构]

### 重构目标
- [明确需要改进的具体方面]
- [保持的设计思路]
- [优化的实现方式]

### 子任务分解
- [基于参考的详细任务分解]

### 风险评估
- [识别重构风险]

### 开发计划
- [具体的时间安排]
```

### 测试先行阶段输出：
```python
# 基于原有接口的测试代码
class Test[ModuleName]Refactor:
    """[模块名称]重构测试用例 - 基于原有接口"""
    
    def test_[原有功能](self):
        """测试[原有功能] - 保持兼容性"""
        # 测试原有接口的兼容性
        pass
    
    def test_[新功能](self):
        """测试[新功能] - 重构优化"""
        # 测试新的优化功能
        pass
```

### 编码实现阶段输出：
```python
# 基于参考思路的实现代码
class [ClassName]:
    """[类描述] - 基于原有设计思路重构"""
    
    def [method_name](self):
        """[方法描述] - 参考原有实现，优化性能"""
        # 基于原有思路的优化实现
        pass
```

### 提交与反馈阶段输出：
```markdown
## ✅ 重构完成总结

### 与原有代码对比
- [保持的设计思路]
- [改进的实现方式]
- [简化的复杂逻辑]

### 测试通过列表
- [ ] test_原有功能_兼容性
- [ ] test_新功能_优化
- [ ] test_性能_提升

### 性能改进
- [具体改进指标]

### 代码质量提升
- [质量改进点]

### 向后兼容性
- [兼容性说明]

### 下一步建议
- [后续任务建议]
```

## 禁止行为
- 完全抛弃原有设计思路
- 不参考GitHub上的实现
- 忽略向后兼容性
- 跳过测试直接编写代码
- 一次性重构多个模块
- 不添加重构思路注释
- 不更新相关文档

## ✅ 必须行为
- 深入分析GitHub上的原有代码
- 保持设计思路的连续性
- 每个功能先写测试
- 基于参考进行优化实现
- 记录重构过程和改进点
- 严格遵循TDD红绿循环
- 添加详细的重构思路注释
- 创建重构思路文档
- 保持代码可读性
- 使用conda环境 ai4qkd_env 进行开发
- 每次开发前激活环境
- 验证环境依赖状态

## 注意事项

### 当前状态提醒
- 代码库已完全清空，需要从零开始实现
- 保留了完整的理论基础和研究文档
- 所有之前验证的研究成果都需要在新架构中重新实现
- 重构过程中要参考GitHub master分支的代码思路
- 每个重构步骤都要有清晰的思路说明

### 环境使用提醒
- 使用已创建的conda环境 `ai4qkd_env`
- 环境路径：`D:\Users\Administrator\miniconda3\envs\ai4qkd_env`
- 每次开发前必须激活环境：`conda activate ai4qkd_env`
- 所有依赖已预装，无需重新安装
- 确保在此环境中进行所有重构工作

### 开发优先级
1. 首先实现最小可行产品 (MVP)
2. 验证核心功能可正常工作
3. 逐步添加高级功能
4. 最后优化性能和用户体验

这个重构项目的目标是创建一个**简洁、高效、易于理解**的AI4QKD系统，让研究人员能够快速上手并扩展功能。重构过程中将严格参考GitHub上的原有实现思路，确保设计连续性和功能完整性。每个重构步骤都将有详细的思路说明和注释，确保代码的可读性和可维护性。