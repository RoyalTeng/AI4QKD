PASS

- §2.1 fix is now citation-clean against KW PDF p.1181-1182: Definition 20.1 only states an `(n,K,ε)` SKA protocol has `p_err(C) ≤ ε`, and the note now correctly separates the Eve-set/capacity comparison as a path-α application-layer step, not KW’s direct wording.
- §2.2 still passes: min/inf monotonicity under `E' ⊆ E` is elementary and the sign direction is correct.
- §2.3 still passes: the quoted KW sentence on invariance under isometric extension matches p.1181 with acceptable ellipsis.
- §3 still passes: `(*)` is unconditional set-monotonicity; `(†)` is explicitly conditional on `L1.G2 [UNKNOWN]`.
- `§1` still correctly flags `P1`, `P2` as `[UNKNOWN]`.
- Scope remains clean: no smuggling of closure for `L2.G3`, `L3.G2`, or `L1.G2`.