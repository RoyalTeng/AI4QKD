# WLC 2018 精读(Level 3+ scoped, v0.3)—— Winick-Lütkenhaus-Coles:Reliable numerical key rates for QKD

**论文引用**:Adam Winick, Norbert Lütkenhaus, Patrick J. Coles.
*Reliable numerical key rates for quantum key distribution*.
Quantum 2:77 (2018). arXiv:1710.05511.

**Zotero Item Key**:`K48S2VUZ`
**精读日期**:2026-04-19(v0.3 同日响应 codex round 2 FAIL;v0.2 响应 round 1 FAIL)
**精读等级**:**Level 3+ scoped**(原 Level 4 目标降档;codex round 2 指出 F4 未闭合 + §5.2 DW↔WLC 桥接式仍 deferred,未达 proof-grade)
**状态**:**draft, 部分 DEFERRED 至人类 PDF 核对 + R1.3 数值复现闭环**

**v0.3 差量**(response to codex round 2):
- §4.2:Thm 1 证明**显式降级为 schematic proof**(非 proof-grade),在 Step 2 添加 "transpose $T$ vs HS 内积" 订正小节说明 v0.2 绕过未解点
- §5.2:DW↔WLC 桥接式仍保留 DEFERRED,但**明确指出这是核心等式**;benchmark 小节显式写 $f_\text{EC}=1$ ideal assumption
- §6.2:Benchmark 表头加入 $f_\text{EC}=1$ 明示 + 单位口径 bit/signal 注记
- §9.3-§9.4:Source C 明确降级为 "internal cross-check",新加 §9.5 "独立证据链未闭合" 声明
- §10:Limitations 小节加入 codex round 2 未闭项

---

## 0. 来源透明度与 codex FAIL 回应

| 数据源 | 访问方式 | 可信度标签 |
|--------|---------|-----------|
| Zotero 全文 | `mcp__zotero__zotero_get_item_fulltext` | ❌ 失败(Zotero 存储 quota 满,PDF 附件未同步到云) |
| arXiv HTML(ar5iv) | `WebFetch` at 2026-04-19 | ✅ **[VERIFIED from ar5iv 2026-04-19]** |
| 原论文 PDF `quantum-journal.org/papers/q-2018-07-26-77/pdf/` | **本 v0.2 未访问** | ⏳ DEFERRED 至人类 PDF 核对 R1.3 期 |
| Claude 训练语料 | 内部回忆 | ⚠️ **[AI-DERIVED, NEEDS HUMAN PDF VERIFY]** |

### v0.1 被 codex FAIL(2026-04-19 round 1)的核心问题

| # | v0.1 错误 | v0.2 修正位置 |
|---|----------|---------------|
| F1 | §2-§5 未按 R1.1 验收条款的 "MS-EB 与 CML 2016 关系 / $\Gamma_k=\gamma_k$ 构造 / primal-dual + gap sanity" 小节组织 | **§2 / §3 / §4 / §5 / §6** 重排,逐条对齐 R1.1 验收 |
| F2 | §6 BB84 Benchmark 只定性描述 "gap ~1e-6",无协议 × 参数 × 文献值 × 本算值对照表 | §6.2 加入可复核 benchmark 表(WLC 2018 Fig.3 的数值锚点) |
| F3 | §7.1 "通道级去极化正则化 $X_\text{reg}=(1-\epsilon)X_\text{raw}+\epsilon\tau$ 直接来自 Thm 2" 错把项目实现等同 WLC 原式(WLC 写 $G_\epsilon = D_\epsilon \circ G$ 而非 $X_\text{reg}$ 矩阵运算) | §7.1 改为 "项目实现是 WLC $D_\epsilon$ 的矩阵实例,非 WLC 原句";标 project-specific adaptation |
| F4 | §11 所谓 "三源一致" 里 2 源是项目内部文档(REFACTORING_PLAN / PHASE0_M1_TECHNICAL_SPEC),非独立验证 | §9 重写:独立源 = 原论文 (ar5iv) + 手推 BB84 子情形 + Coles-Metodiev-Lütkenhaus 2016 前传(独立论文) |

### RESEARCH_PLAN §1.2 补丁 2b 拓扑适用性

- **拓扑前提**:WLC 2018 针对有限维 Hilbert space 上的 prepare-and-measure / EB DV-QKD,**不涉及无限维 bosonic 信道**。用于 Sub-Q1 M1 BB84(qubit)合法。
- **信任假设**:Devetak-Winter + collective attack + de Finetti;测量信任可部分降低,源态已知
- **协议自由度**:渐近率 + one-way post-processing,**不含跨轮 memory / finite-key**
- **精确引用**:以下每条命题给 Theorem / Eq. 编号(ar5iv 版);标 `[?]` 处尚未核对原 PDF 页码。

---

## 1. 一句话总结

WLC 2018 把 Devetak-Winter 公式 $R^\infty \geq p_\text{pass}[H(Z^R | E A B) - \text{leak}^\text{EC}]$ 的关键量 $H(Z^R | E A B)$,通过两个可显式构造的 CPTNI / CPTP 映射 $\mathcal{G}$ 和 pinching channel $\mathcal{Z}$,**化为** 在观测约束 $S = \{\rho : \text{Tr}(\Gamma_k \rho) = \gamma_k\}$ 上的相对熵最小化

$$f(\rho) := D\!\big(\mathcal{G}(\rho) \,\|\, \mathcal{Z}(\mathcal{G}(\rho))\big)$$

然后给出**可靠下界定理**(Thm 1–3):通过凸函数的**线性化 + 对偶证书**,把 $\min_\rho f$ 这个非线性凸问题化为**任意可行 $\rho$ 处的数值下界** $\beta(\rho) \leq \alpha := \min_{\rho \in S} f(\rho)$。奇异态(QBER=0)与有限精度各自由 Thm 2 / Thm 3 处理。

---

## 2. MS-EB 框架与 Coles-Metodiev-Lütkenhaus 2016(R1.1 验收项 §2)

### 2.1 CML 2016 前传的贡献

Coles-Metodiev-Lütkenhaus 2016 *Nat.Commun.* 7:11712(Zotero `B8WX65DA`,Level 3 前置精读):

- **首次**把数值 QKD key rate 化为 $\min_\rho D(G(\rho) \| Z(G(\rho)))$ 的 SDP
- $G$ 是 Alice 测量 + Bob 测量 + 宣告 + 筛选的 CPTNI 映射
- $Z$ 是 pinching channel,把 Alice 的 raw key register 经典化
- **问题**:CML 只保证*数值解收敛*于全局最优,无显式证书 — 数值松弛过大时 key rate 被高估(不安全)

### 2.2 WLC 2018 相对 CML 的三个关键改进

1. **Thm 1(Reliable Lower Bound)**:对**任意** $\rho \in S$,$\beta(\rho) \leq \alpha$ 恒成立 ⇒ 数值求解即使未到全局最优,输出仍是合法下界(**安全优先**)
2. **Thm 2(Singular-state Robustness)**:QBER=0 时 $\mathcal{G}(\rho)$ 秩亏 $\Rightarrow \log$ 奇异 $\Rightarrow f$ 未定义;正则化 $\mathcal{G}_\epsilon = D_\epsilon \circ \mathcal{G}$ 把问题拉回 strictly positive
3. **Thm 3(Finite Precision)**:求解器数值 tolerance $\epsilon'$ 之内的约束违反仍保下界

### 2.3 MS-EB 与 WLC 的口径对接(本项目 PROSPECTUS §4.1)

WLC 2018 的"Alice 源 + channel + Bob 测量 + announcement + sift"对应 MS-EB 五元组 $(\mathcal{P}, \mathcal{E}, \mathcal{A}, \mathcal{T}, \mathcal{K})$:

| WLC 对象 | MS-EB 对应 | BB84 具体(见 `docs/msen/bb84-formulation.md` v0.2) |
|----------|------------|------------------------------------------------------|
| Alice source state | $\mathcal{P} = (P_1)$ | $\ket{\psi_1}_{A'A}$(§2.2) |
| channel | $\mathcal{E}$ | 对称去极化 $\mathcal{E}_\text{ch}$(§3.2) |
| announcement + sift | $\mathcal{A}$ | $\text{publish}(\theta_B) + \text{sift\_keep}$(§4) |
| acceptance | $\mathcal{T}$ | M1 恒 accept |
| key map | $\mathcal{K}$ | §6,Kraus DEFERRED to R1.3 |

---

## 3. 观测约束 $\Gamma_k = \gamma_k$ 的构造(R1.1 验收项 §3)

### 3.1 构造规则(WLC §II)

对 EB 协议,$\Gamma_k$ 是 Hermitian 算子作用在 $\mathcal{L}(\mathcal{H}_{AB})$ 上,对应某个物理可观测的统计量:

1. **Z 基 QBER**:$\Gamma_{\text{qber\_Z}} = P_{\text{Z-error}}$,其中 $P_{\text{Z-error}}$ 是 "Alice 与 Bob 都测 Z,结果不同" 的投影子
2. **X 基 QBER**:$\Gamma_{\text{qber\_X}} = P_{\text{X-error}}$,类似
3. **Gain** / 其他:协议特定的额外约束(M2 MDI / M3 decoy 涉及)

### 3.2 BB84 具体形式(与 `bb84-formulation.md` v0.2 §8 一致)

$$\Gamma_{\text{qber\_Z}} = \proj{01}_{AB} + \proj{10}_{AB}$$

$$\Gamma_{\text{qber\_X}} = H_A H_B (\proj{01} + \proj{10}) H_A H_B$$

**观测值** $\gamma_{\text{qber\_Z}} = \gamma_{\text{qber\_X}} = e$(symmetric depolarization QBER = e)。

### 3.3 可行集 $S$

$$S = \{\rho \in \mathcal{L}(\mathcal{H}_{AB}) : \rho \succeq 0,\; \text{Tr}(\rho) = 1,\; \text{Tr}(\Gamma_k \rho) = \gamma_k \; \forall k \}$$

- 迹约束:线性仿射
- 正半定:凸锥
- 观测约束:线性仿射等式

故 $S$ 是凸紧集;在 Slater 内点条件下强对偶成立。

### 3.4 BB84 SDP 的变量维度 + 约束列表(R1.1 验收项)

| 对象 | 维度 / 基数 |
|------|-------------|
| SDP 变量 $\rho$ | $4 \times 4$(A qubit × B qubit)|
| Hermitian 自由度 | $4^2 = 16$ 实数 |
| 迹约束 | 1 个线性等式 |
| 观测约束($\Gamma_{\text{qber\_Z}}, \Gamma_{\text{qber\_X}}$) | 2 个线性等式 |
| 半定锥 | 1 个 $\rho \succeq 0$ |
| 目标函数 | $f(\rho) = D(\mathcal{G}(\rho) \| \mathcal{Z}(\mathcal{G}(\rho)))$(nat) |

变量总自由度 16,等式约束 3,故可行集是 13 维仿射子空间与 PSD 锥的交。

---

## 4. Theorem 1 - 3 的陈述 + primal/dual + gap sanity(R1.1 验收项 §4–§5)

### 4.1 Theorem 1 (Reliable Lower Bound)**[VERIFIED from ar5iv 2026-04-19]**

> **陈述**:Given any $\rho \in S$, if $\nabla f(\rho)$ exists, then
> $$\alpha := \min_{\rho^* \in S} f(\rho^*) \geq \beta(\rho) := f(\rho) - \text{Tr}(\rho^T \nabla f(\rho)) + \max_{y \in S^*(\rho)} \gamma \cdot y$$

**$S^*(\sigma)$ 定义**:Lagrangian 对偶可行集

$$S^*(\sigma) := \{y \in \mathbb{R}^K : \nabla f(\sigma) - \sum_k y_k \Gamma_k \succeq 0\}$$

**证明 5 步**(§4.2 下)

### 4.2 Theorem 1 证明重现(**schematic proof,v0.3 降级**)

**v0.3 降级说明**(codex round 2 F1):Thm 1 原陈述带 $\text{Tr}(\rho^T \nabla f(\rho))$ 的**转置 $T$**,v0.2 的"证明"直接按无转置的 HS 内积写完,绕过了这个点。本节现在明示为 **schematic proof** —— 步骤骨架 + HS 内积版本正确,但 $\rho^T$ vs $\rho$ 的 WLC 原句转置未真正闭合(见 §4.2.5)。真正 proof-grade 重现待 R1.3 期人类 PDF 精读 WLC §III 的梯度定义约定后补。

**Step 1(切平面下界)**:$f$ 凸(Lindblad 相对熵联合凸 + $\mathcal{G}, \mathcal{Z}$ 线性)

$$f(\rho^*) \geq f(\sigma) + \langle \nabla f(\sigma), \rho^* - \sigma \rangle_{\text{HS}}$$

**Step 2(HS → Tr,无转置版本)**:$\nabla f(\sigma)$ Hermitian,$\langle X, Y \rangle_{\text{HS}} = \text{Tr}(X^\dagger Y) = \text{Tr}(X Y)$,故(**相对 WLC 原式,本步绕过了转置 $T$**)

$$\alpha \geq f(\sigma) - \text{Tr}(\sigma \nabla f(\sigma)) + \text{Tr}(\rho^* \nabla f(\sigma))$$

**Step 3(放宽为 $\min_{\rho \in S}$)**:

$$\text{Tr}(\rho^* \nabla f(\sigma)) \geq \min_{\rho \in S} \text{Tr}(\rho \nabla f(\sigma)) \tag{†}$$

**Step 4(SDP 对偶化)**:$\min_\rho$ on $S$ 的 Lagrangian

$$L(\rho, y, \Lambda) = \text{Tr}(\rho \nabla f(\sigma)) - \sum_k y_k (\text{Tr}(\Gamma_k \rho) - \gamma_k) - \text{Tr}(\Lambda \rho)$$

($\Lambda \succeq 0$ 对应 PSD 约束 $\rho \succeq 0$)

内层 $\min_\rho L$ 为 $-\infty$ 除非 $\nabla f(\sigma) - \sum_k y_k \Gamma_k - \Lambda = 0$,此时 $\min_\rho L = \sum_k y_k \gamma_k$。

消去 $\Lambda \succeq 0$ 给 $\nabla f(\sigma) - \sum_k y_k \Gamma_k \succeq 0$ 的约束,即 $S^*(\sigma)$。Strong duality(Slater)下

$$\min_{\rho \in S} \text{Tr}(\rho \nabla f(\sigma)) = \max_{y \in S^*(\sigma)} \gamma \cdot y$$

**Step 5(代回 †)**:$\alpha \geq \beta(\sigma)$ $\square$(**schematic**,未含原式 $\rho^T$)

**Primal-dual gap sanity**:取 $\sigma = \rho^*$(若能到最优),由 KKT 条件 $\nabla f(\rho^*) = \sum_k y_k^* \Gamma_k + \lambda I$(含迹约束的乘子 $\lambda$),可推 $\beta(\rho^*) = \alpha$ — 下界紧 ✓。**这是 "reliable" 的精髓**:$\beta(\rho)$ 与 $\alpha$ 之间的 gap $= \alpha - \beta(\rho) \geq 0$ 可任意接近 0(当 $\rho \to \rho^*$)。

#### 4.2.5 关于原式 $\rho^T$ 的未解点(v0.3 codex round 2 显式标注)

WLC 原式 $\beta(\rho) = f(\rho) - \text{Tr}(\rho^T \nabla f(\rho)) + \max_y \gamma \cdot y$ 的 $T$ 存在两种可能的约定:

- **(a) 真转置**:在特定 matrix-calculus 约定下(把 Hermitian 矩阵视作 $d^2$ 维实向量的梯度 $\partial f / \partial \rho$),梯度与变量的配对是 $\text{Tr}(\rho^T \cdot)$ 而非 $\text{Tr}(\rho \cdot)$。此时 $\nabla f(\rho)$ 的定义 $\ne$ Hermitian 算符 operator derivative,需要展开复杂约定。
- **(b) Hermitian 梯度**:若 $\nabla f(\rho)$ 按 Wilde 2013 §11 的 quantum entropy gradient 约定为 Hermitian 算符(即 $\partial f / \partial \rho^\dagger$ 的共轭),则 $\text{Tr}(\rho^T \nabla f) = \text{Tr}(\rho (\nabla f)^T) = \text{Tr}(\rho (\nabla f)^*)$;若 $\nabla f$ 实对称,则 $= \text{Tr}(\rho \nabla f)$,退化为 §4.2 Step 2 的 HS 内积。

**未解**:原论文采用 (a) 还是 (b)?v0.3 未读原 PDF,ar5iv 在 Thm 1 句只写 $\text{Tr}(\rho^T \nabla f)$ 不澄清梯度约定。**R1.3 实施期人类读 WLC §III 的 matrix calculus setup** 即可闭合。

**数值影响**:对 BB84 QBER ∈ [0, 0.1] 范围,$\rho^* = \rho_{AB}$(Bell-diag + symmetric depolarization)是实对称的,$\rho^T = \rho$,$(a) \equiv (b)$ 数值退化无差异。即:v0.3 **对 M1 BB84 benchmark 的正确性不构成威胁**,但 proof-grade / theorem-level 重现未达。

### 4.3 Theorem 2 (Robust Bound for Singular States)**[VERIFIED from ar5iv 2026-04-19]**

> **陈述**:If $0 < \epsilon \leq 1/[e(d'-1)]$, then
> $$\alpha \geq \beta_\epsilon(\rho) - \zeta_\epsilon,\quad \zeta_\epsilon = 2\epsilon(d'-1) \log\!\left(\frac{d'}{\epsilon(d'-1)}\right)$$

**正则化**(WLC 原式,**非项目代码实现**):

$$\mathcal{G}_\epsilon := D_\epsilon \circ \mathcal{G},\qquad D_\epsilon(\sigma) := (1-\epsilon)\sigma + \epsilon \text{Tr}(\sigma) \frac{I_{d'}}{d'}$$

$d' = \dim(\mathcal{H}_\text{out of } \mathcal{G}) = \dim(\mathcal{H}_{A_\text{key}} \otimes \mathcal{H}_{B_\text{side}})$

**BB84 $d'$**:此值依赖 $\mathcal{G}$ 的精确构造,DEFERRED 到 R1.3 PDF 核对(§6)。粗估 $d' \leq d_{A_\text{key}} \cdot d_B \leq 2 \cdot 2 = 4$(有些文献取 $d' = 8$ 把 A register 原始空间 4 也合进,需 PDF 核对)。

**项目实现的对应**(**adaption,非 WLC 原句**):REFACTORING_PLAN §4.6 的

```
X_reg = (1 - epsilon) * X_raw + epsilon * Tr(X_raw) * tau
```

**不是** WLC 定义里的 $\mathcal{G}_\epsilon$ 映射,而是在 $\mathcal{G}$ **作用后**对 $X = \mathcal{G}(\rho)$ 直接套的矩阵级操作。两者等价当 $\mathcal{G}$ 线性 trace-preserving,但形式上是:

- WLC:$\mathcal{G}_\epsilon = D_\epsilon \circ \mathcal{G}$(map-composition)
- 项目:$\mathcal{G}(\rho) \to D_\epsilon(\mathcal{G}(\rho))$(point-wise regularization)

对 CPTP $\mathcal{G}$ 两者完全等价;对 CPTNI $\mathcal{G}$(trace 可能 $<1$)需要显式保持 trace 对齐。**项目实现是 project-specific adaptation,非 WLC 原文公式**。

### 4.4 Theorem 3 (Finite Precision Tolerance)**[VERIFIED from ar5iv 2026-04-19]**

> **陈述**:若 $\rho \in \tilde{S}_{\epsilon'} := \{\rho : |\text{Tr}(\tilde\Gamma_k \rho) - \tilde\gamma_k| \leq \epsilon'\}$,则
> $$\alpha \geq \beta_{\epsilon,\epsilon'}(\rho) - \zeta_\epsilon$$

**语义**:CVXPY / MOSEK / CLARABEL 的数值 tolerance 允许 $|\text{Tr}(\Gamma_k \rho) - \gamma_k| \leq \epsilon'$;Thm 3 让 "optimal_inaccurate" 结果仍构成合法下界。

### 4.5 $\mathcal{G}$ 的 Kraus / $\mathcal{Z}$ 的 pinching 显式形式 —— DEFERRED

WLC Eq. (46)–(56) 给 $\mathcal{G} = V \Pi \mathcal{A}(\sigma) \Pi V^\dagger$ 的分解。**v0.2 本节 DEFER** 到 R1.3 实施期人类 PDF 精读($\S 6$ 具体动作)。占位 schematic:

- $\mathcal{A}$:Alice 本地 POVM(基选择 dilation)
- $\Pi$:sift-keep projector(basis-matched branch)
- $V$:key register 嵌入 isometry
- $\mathcal{G}(\sigma) = V \Pi \mathcal{A}(\sigma) \Pi V^\dagger$
- $\mathcal{Z}(\tau) = \sum_x \proj x_{A_\text{key}} \tau \proj x_{A_\text{key}}$(延拓至 $B_\text{side}$ 上恒等)

**R1.3 验收数值自检**(与 `bb84-formulation.md` v0.2 §9 同):
- QBER=0:$D(\mathcal{G}(\rho) \| \mathcal{Z}(\mathcal{G}(\rho))) \leq 10^{-8}$(0 up to 正则化)
- QBER=0.05:$D(\cdot\|\cdot) \in [h(0.05) - 10^{-3}, h(0.05) + 10^{-3}]$,$h(0.05) = 0.2864$

---

## 5. Devetak-Winter 与 WLC 的桥接(R1.1 验收项 §5)

### 5.1 Devetak-Winter 公式

$$R_{\text{DW}} = p_\text{pass} \cdot [H(Z^R | E A B)_\rho - f_\text{EC} \cdot H(Z^R | Z^B)_\rho]$$

### 5.2 WLC 目标函数 $f(\rho)$ 与 $H(Z^R|E)$ 的关系(**核心桥接,DEFERRED**)

**v0.3 明确标注**(codex round 2 F2):本式是 DW↔WLC 桥接的核心等式,proof-grade 文档不能把它留在 `[AI-DERIVED]` 状态。v0.3 仍保留 DEFERRED,但明示此式的闭合是下一轮工作的**硬门槛**。待 R1.3 实施期人类对齐 WLC Eq. (49)–(55) 或等价 Coles 2012(arXiv:1109.1267)/ Devetak-Winter 2005 PRSA 461 链条后补。

WLC 给出恒等式(§III,**[AI-DERIVED, exact sign + nat/bit 单位 + $A$ 指代是 $A_\text{key}$ 还是 purification register 均待 PDF 验证]**):

$$H(Z^R | E A B) = \log_2 d_{A_\text{key}} - \frac{1}{\ln 2} \cdot D(\mathcal{G}(\rho) \| \mathcal{Z}(\mathcal{G}(\rho)))$$

**逐项 nat/bit 说明**(v0.3 新增):
- $\log_2 d_{A_\text{key}}$ 以 **bit** 为单位($\log_2 2 = 1$ bit)
- $D(\cdot \| \cdot)$ 以 **nat** 为单位(Lindblad 相对熵使用自然对数)
- $\frac{1}{\ln 2} \cdot D$ 把 nat 转 bit
- 整式两侧以 **bit** 为单位

即:$D$ 部分 = Eve 对 Alice key 获取的 Holevo information(nat);$\log_2 d - D/\ln 2$ = Alice key 留给 Eve 的条件熵(bit)。

对 BB84 $d_{A_\text{key}} = 2$:$H(Z^R | E) = 1 - D / \ln 2$(bit)。

**R1.3 验证项**(人类 PDF 精读必做):
1. WLC Eq. 编号(估 Eq. 49-55)对齐
2. sign 是 `log - D/ln2`(v0.3 写法)还是 `log - D·log₂ e`(等价);$\log_2 e = 1/\ln 2$ 二者等价
3. $A$ 指 $A_\text{key}$ register 还是 Alice 原始多轮 register(维度对齐)
4. 是否有 Alice ancilla 纯化项未显式写出

### 5.3 拼装

$$\boxed{R = p_\text{pass}\!\left[\log_2 d_{A_\text{key}} - \frac{\min_{\rho \in S} D(\mathcal{G}(\rho)\|\mathcal{Z}(\mathcal{G}(\rho)))}{\ln 2} - f_\text{EC} h(e)\right]}$$

### 5.4 v0.1 的拼装公式错误**记在 v0.2 订正**

v0.1 §10.3 写 "$R = p_\text{sift} [f^*/\ln 2 - f_\text{EC} h(e)]$" 漏了 $\log_2 d_{A_\text{key}}$ 项,导致 QBER=0 算成 $-f_\text{EC} h(0) = 0$(错误的 0 bit/signal)。v0.2 §5.3 + `bb84-formulation.md` v0.2 §9 修正。

---

## 6. BB84 Benchmark(R1.1 验收项 §6)

### 6.1 WLC 2018 §V 公开的 3 个 benchmark 应用

**[VERIFIED from ar5iv 2026-04-19]**:WLC 给 3 个数值测试,每个都跟独立文献对比:

| # | Benchmark | 对照文献 | WLC 数值 vs 对照 |
|---|-----------|----------|-------------------|
| B1 | Detector efficiency mismatch | Fung-Tamaki-Qi-Chen-Ma 2009 | 匹配,gap `≈ 1e-6` |
| B2 | Trojan-horse attack | Lucamarini-Choi-Ward-Dynes-Yuan-Shields 2015 | 匹配,gap `≈ 1e-6` |
| B3 | Phase-coherent BB84 | Lo-Preskill 2007 | 匹配,gap `≈ 1e-6` |

**"gap"**:primal-dual gap(Thm 1 的 $\alpha - \beta(\rho)$)在最优附近的数值距离;作为 SDP 求解精度指标。

### 6.2 M1 benchmark 对照表(R1.3 验收硬指标)

**口径声明**(v0.3 明示,codex round 2 下一轮建议 2):
- **单位**:bit/signal(非 bit/sift);$R = p_\text{sift}\cdot R_\text{per\_sift}$
- **$f_\text{EC}$**:benchmark 表内 **$f_\text{EC} = 1$(ideal EC)**,对应 Shor-Preskill 闭式 $R = p_\text{sift}(1-2h(e))$。M3 / 实际 LDPC 纠错的 $f_\text{EC} \approx 1.16$ **不**进 R1.3 benchmark,留给 M3 decoy 期的 realistic benchmark 单独表。
- **QBER 范围**:$e \in [0, 0.11]$,覆盖 BB84 asymptotic cutoff $e^* = 11\%$ 附近

对 BB84 symmetric depolarization,R 对 QBER 的闭式解析参考(Shor-Preskill 2000,$f_\text{EC}=1$ ideal):

$$R_{\text{SP}}(e) = p_\text{sift} (1 - 2 h(e)),\quad p_\text{sift} = 0.5,\quad f_\text{EC} = 1$$

M1 R1.3 的 `test_wlc_bb84_matches_shor_preskill` 预期数值(bit/signal,$f_\text{EC}=1$):

| QBER $e$ | $h(e)$ | $R_{\text{SP}}(e)$ | WLC SDP 预期 | 容差 |
|----------|--------|---------------------|---------------|------|
| 0.000 | 0.0000 | 0.5000 | 0.5000 | `abs=5e-4` |
| 0.010 | 0.0808 | 0.4192 | 0.4192 | `abs=5e-4` |
| 0.020 | 0.1414 | 0.3586 | 0.3586 | `abs=5e-4` |
| 0.050 | 0.2864 | 0.2136 | 0.2136 | `abs=5e-4` |
| 0.080 | 0.4022 | 0.0956 | 0.0956 | `abs=5e-4` |
| 0.100 | 0.4690 | 0.0620 | 0.0620 | `abs=5e-4` |
| 0.110 | 0.4999 | 0.0002 | 约 0 | `abs=5e-4` |

(容差按 MOSEK 主线 `rel=0.01, abs=5e-4`;CLARABEL fallback 时 `rel=0.02, abs=1e-3`)

### 6.3 QBER=0 手算(Level 4 独立重推)

用 `bb84-formulation.md` v0.2 §7 的 $\rho_{AB}(p=0) = \proj{\Phi^+}$ 代入 §5.3:

- $\min D = 0$(QBER=0 时 Alice-Bob 完美相关 ⇒ Eve 无信息 ⇒ Holevo $= 0$)—— 形式验证见 `bb84-formulation.md` v0.2 §10
- $h(0) = 0$
- $R = 0.5 \cdot [1 - 0/\ln 2 - 0] = 0.5$ bit/signal ✓

### 6.4 QBER=0.05 手算

- $\min D = h(0.05) \cdot \ln 2 = 0.2864 \cdot 0.6931 = 0.1985$ nat  
  (这里 $\min D$ 等于 $h(e) \ln 2$ 是 BB84 symmetric depolarization 在 Z + X 基对称约束下的已知解析解,出自 entropic UR / Devetak-Winter 收敛)
- $h(0.05) = 0.2864$
- $R = 0.5 \cdot [1 - 0.1985/0.6931 - 0.2864] = 0.5 \cdot [1 - 0.2864 - 0.2864] = 0.5 \cdot 0.4272 = 0.2136$ bit/signal ✓

与 $R_\text{SP}(0.05) = 0.2136$ 严格一致(二者都是 BB84 SDP 的解析最优)✓

---

## 7. 项目实现中的 WLC 与 project-specific adaptation 的边界(codex F3 修正)

### 7.1 REFACTORING_PLAN §4.6 的通道级去极化正则化

**项目实现**(代码,REFACTORING_PLAN §4.6):

```python
X_raw = _apply_cvxpy_map(G.map, rho)                # G(ρ)
X_reg = (1 - epsilon) * X_raw + epsilon * cp.trace(X_raw) * tau_over_d
# tau_over_d = I_{d'} / d'
```

**WLC 原文**(Thm 2, Eq. 25):$\mathcal{G}_\epsilon := D_\epsilon \circ \mathcal{G}$,$D_\epsilon(\sigma) = (1-\epsilon)\sigma + \epsilon \text{Tr}(\sigma) I_{d'}/d'$。

**关系**:两种写法对 CPTP $\mathcal{G}$ 等价:$(D_\epsilon \circ \mathcal{G})(\rho) = D_\epsilon(\mathcal{G}(\rho))$ 的 composition 等于 point-wise evaluation。项目实现在 $\mathcal{G}$ 作用后的 matrix variable 上做 $D_\epsilon$,不是 map-composition 形式。

**Adaptation rationale**:CVXPY 层级,直接在 `cp.Variable` 表达式上做 $D_\epsilon$ 更自然(无需定义"复合 Kraus map"对象)。数值上等价,形式上不同。

**v0.1 的不当等同**:原 §7.1 写"通道级去极化正则化**直接来自** Thm 2",误把项目矩阵运算写成 WLC 原句。v0.2 订正:项目实现是 WLC 原理的**工程改写**(project-specific adaptation),不是 WLC 的原文公式。

### 7.2 $\epsilon$ 取值的数值 sanity

Thm 2 要求 $\epsilon \leq 1/[e(d'-1)]$。对 BB84 $d' \leq 4$,$\epsilon_\text{max} \geq 1/(e \cdot 3) \approx 0.123$。REFACTORING_PLAN 取 $\epsilon = 10^{-9}$,合规 ✓。

$\zeta_\epsilon \approx 2 \cdot 10^{-9} \cdot 3 \cdot \log(4/(3 \cdot 10^{-9})) = 6 \cdot 10^{-9} \cdot 20.6 \approx 1.2 \times 10^{-7}$ — 远低于 `abs=5e-4` 容差。

---

## 8. 对本项目的 Bearing

### 8.1 M1 Sub-Q1

- **Thm 1** → REFACTORING_PLAN §4.6 `wlc_key_rate` 入口
- **Thm 2** → §4.6 通道正则化实现(§7.1)
- **Thm 3** → §附录 B "accept optimal_inaccurate" 数值放宽

### 8.2 Sub-Q3 上界方向不使用 WLC

WLC 是 achievability(下界)converse。Sub-Q3 用 PLOB / Pirandola 2019 / Khatri-Wilde 2020,不经 WLC 路径。

### 8.3 对 FINDINGS v2 的影响

无直接影响。FINDINGS v2 的可达率引用 TF-QKD / PM-QKD [THM],不过 WLC。

---

## 9. 证据链评估(v0.3 降级:internal cross-check + 独立证据链未闭合)

**v0.3 降级说明**(codex round 2 F4):v0.2 §9 标题"独立证据链"过强。§9.3 的 "BB84 手推" 实际上依赖本文件 §5.3 的拼装公式,属于**本文件内部 sanity check**(算得通),不构成对 §5.2 桥接式的独立外部证据。v0.3 重命名并明示独立证据链**未闭合**(见 §9.5)。

### 9.1 Source A:原论文 ar5iv HTML(VERIFIED 2026-04-19)—— **External**

WLC 2018 §II 显式 $f(\rho) = D(\mathcal{G}(\rho)\|\mathcal{Z}(\mathcal{G}(\rho)))$,Thm 1 句 $\beta(\rho) \leq \alpha$。

### 9.2 Source B:CML 2016 前传(Coles-Metodiev-Lütkenhaus)—— **External(独立论文)**

Coles-Metodiev-Lütkenhaus 2016 *Nat.Commun.* 7:11712 §II-A 给出相同形式 $\min D(G(\rho)\|Z(G(\rho)))$,但无 Thm 1 样的 reliability 证书。WLC 2018 是 CML 2016 在"safe lower bound"维度的 follow-up,两篇核心目标函数一致 ✓

### 9.3 ~~Source C~~ Internal cross-check C(v0.3 重命名):BB84 手推一致性

**v0.3 订正**:v0.2 误把本项称为 "Source C 独立证据"。实际上本项从 $\rho_{AB}(e=0) = \proj{\Phi^+}$ 用本文件 §5.3 公式推 $R = 0.5$,属于**本文件内部 sanity check**——验证 §5.3 拼装公式在 QBER=0 极限的数值自洽,**不构成对 §5.2 桥接式的独立论证**。

该 cross-check 的价值:
- 排除 §5.3 拼装公式的低级笔误($\log_2 d_{A_\text{key}}$ 项漏算、$\ln 2$ 系数反转等)
- 与 Shor-Preskill 2000 Thm 1 数值 ($R_\text{BB84}(e=0) = 0.5$) 一致 ✓

**但**这个一致性**无法**升级为 §5.2 的独立证据,因为 QBER=0 是极限退化点($D=0$),任何合理的公式在此点都会给 $0.5$ bit/signal。

### 9.4 Internal cross-check D:numpy 枚举(详见 `bb84-formulation.md` §10.4)

numpy 从 Bell 态定义直接算 $\rho_{AB}$ 和 $\text{Tr}(\Gamma\rho)$,**属于对 §7-§8 自身代数**的原子级自洽检查,仍然是 internal cross-check。

### 9.5 独立证据链**未闭合**声明(v0.3 明示)

**未闭合项**(需要外部真独立源):
1. §5.2 DW↔WLC 桥接式 $H(Z^R|EAB) = \log_2 d - D/\ln 2$ 的 sign/单位/register 约定,需 Shor-Preskill 2000 或 Berta-Christandl-Colbeck-Renes-Renner 2010 或 Devetak-Winter 2005 原文级佐证
2. §4.1 Thm 1 的 $\rho^T$ transpose 约定,需 WLC 原 PDF §III 的 matrix-calculus 定义
3. Eq. (46)–(56) BB84 worked example 的 $\mathcal{G}/\mathcal{Z}$ Kraus,需原 PDF

**闭合路径**:R1.3 实施期(Week 2-3)人类读原 PDF + 跑 SDP 数值验证 `abs_gap ≤ 5e-4`,两路同时收敛才构成闭环。v0.3 距 Level 4 / proof-grade 仍有此 gap。

---

## 10. Limitations

### 10.1 v0.2 未完成项

1. **原 PDF 未亲读**:ar5iv 抓到 Thm 陈述,但:
   - Thm 1 中 $\sigma^T$ 是笔误还是刻意(转置 vs 共轭),v0.1 已疑点,v0.2 未解
   - Thm 2 的 $\zeta_\epsilon$ Fannes-Audenaert 来源与系数 $2(d'-1)$ 的 PDF 页码
   - Thm 3 的 $\|y\|$ bound 显式形式
   - Eq. (46)–(56) 的 $\mathcal{G}$ Kraus 对 BB84 的 worked example

2. **$\mathcal{G}, \mathcal{Z}$ BB84 Kraus DEFERRED**(§4.5 明示)—— 阻塞 R1.3 SDP 实际跑通

3. **QBER=0.05 的 $\min D = h(e) \ln 2$ 论断**:§6.4 使用此解析事实,来源 entropic UR + Devetak-Winter 收敛,但本文未 re-derive;[SYN],需要 CCK 2012 / Berta 2010 查漏

### 10.2 技术风险(补 REFACTORING_PLAN §8)

- `cp.quantum_rel_entr` 在 Hermitian affine 表达式上的 DCPError 风险 → §4.6 已做 Hermitian 投影
- Thm 2 $\epsilon$ 在更高维度协议(M2 MDI / M3 decoy)的合法性随 $d'$ 减小,M2/M3 要重估
- $\zeta_\epsilon$ 项对 M2/M3(更大 $d'$)不一定低于 `abs=5e-4`,需每协议单独评估

### 10.3 项目外部依赖

- Zotero 存储 quota 不解决 → PDF 长期不可访问 → [AI-DERIVED] 项无法升级为 VERIFIED
- 项目 `cvxpy==1.5.*` 的 `quantum_rel_entr` ABI 稳定性未 smoke test(R1.3 开始时第一 action)

---

## 11. 下次读相关文献时需要带上的预备知识

- **Coles-Metodiev-Lütkenhaus 2016 前传**(`B8WX65DA`)**Level 3 精读先行** — 建立 numerical key rate 框架的原型,R1.3 之前必做
- **Devetak-Winter 2005** collective attack 渐近率公式(§5 深度依赖)
- **Fannes-Audenaert 连续性**(Wilde 2013 Ch. 11 / Nielsen-Chuang):Thm 2 $\zeta_\epsilon$ 源
- **Boyd-Vandenberghe Ch. 2–5 Convex Optimization**:Strong duality, Slater 条件
- **Coles-Colbeck-Kaniewski 2012 PRL 108:200403**:entropic UR 形式,§10.2 in `bb84-formulation.md` 引用

---

## 12. AI 协助范围声明(RESEARCH_PLAN §7.3)

### 12.1 Claude Opus 4.7 本次(v0.1 + v0.2)的工作

- 通过 ar5iv WebFetch 抓取 WLC 2018 核心陈述(Thm 1-3,$f(\rho)$,Eq. (55) 片段)
- §4.2 Thm 1 convex-analysis 5 步证明(标准推导)
- §6.3-§6.4 BB84 手算
- §7.1 项目实现 vs WLC 原式的形式边界澄清(v0.2 codex 响应)
- §9 三源独立证据链重构(v0.2 codex 响应)

### 12.2 必须人类研究者(Teng, Jun)sign-off

1. WLC 2018 原 PDF 精读 Eq. (46)–(56) 并给 BB84 的 $\mathcal{G}$ Kraus 显式
2. 核对 Thm 2 $\zeta_\epsilon$ 系数推导
3. Coles-Metodiev-Lütkenhaus 2016 Level 3 精读(前传,Zotero `B8WX65DA` 已在库)
4. 签字后本文件 + `bb84-formulation.md` 共同作为 R1.3 实施权威参考

### 12.3 未使用

- Web 搜索(v0.2 修订期,仅 v0.1 用 ar5iv WebFetch)
- Zotero 全文(PDF 未同步,quota 满)

---

## Changelog

- **v0.2**(2026-04-19, codex FAIL 响应):
  - §2-§5 重排对齐 R1.1 验收条款(MS-EB / CML 关系、$\Gamma_k$ 构造、primal-dual + gap sanity、DW 桥接)
  - §6.2 新增 BB84 benchmark 数值表(7 个 QBER 点)
  - §7.1 澄清 project-specific adaptation 与 WLC 原式的边界(codex F3)
  - §9 重写独立证据链(Source A ar5iv + Source B CML 2016 前传 + Source C 手推)(codex F4)
  - §4.5 与 §10.1 明示 $\mathcal{G}$ Kraus DEFERRED
  - 订正 v0.1 §5.3 拼装公式漏 $\log_2 d_{A_\text{key}}$ 项(codex 未直接指出但本次 self-audit 发现)
- **v0.1**(2026-04-19):首次落盘。codex FAIL 见 §0。

---

*R1.1 WLC-2018 精读 v0.2 结束。*
